/*
#include "CheckNormal.h"
	position pos1;
	position pos2;
	Normal_Board ** Board = new Normal_Board * [BOARDHEIGTH + 2];
	InitBoard(Board);
	do
	{
		OutputBoard(Board, BOARDHEIGTH + 2 , BOARDWIDTH + 2 );
		cin >> pos1.x >> pos1.y;
		cin >> pos2.x >> pos2.y;
		ConverToNull(Board, pos1.x, pos2.x, pos1.y, pos2.y, BOARDHEIGTH + 2, BOARDWIDTH + 2);
	} while (!CheckEndGame(Board, BOARDHEIGTH + 2, BOARDWIDTH + 2));
	cout << "End Game!" << endl;
	DeleteBoard(Board);
*/ 	
	